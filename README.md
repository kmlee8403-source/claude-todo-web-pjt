# Claude Todo Web Project

Claude Code와 함께 만든 심플한 할 일 목록(Todo List) 웹 앱입니다.
별도 설치나 서버 없이 `index.html` 파일 하나로 동작합니다.

## ✨ 주요 기능

- 할 일 추가 / 수정 / 삭제
- 완료 체크 (완료 처리)
- 카테고리별 분류
- 필터 탭으로 보기 전환 (전체 / 진행중 / 완료 등)
- 진행률(progress) 표시
- 브라우저 `localStorage`에 자동 저장 → 새로고침해도 데이터 유지

## 🛠 기술 스택

- HTML / CSS / Vanilla JavaScript (프레임워크 없음)
- 데이터 저장: 브라우저 `localStorage`

## 🚀 사용 방법

1. 이 저장소를 clone 하거나 `index.html` 파일을 다운로드합니다.
2. `index.html` 파일을 웹 브라우저로 엽니다.
3. 별도 설치나 빌드 과정 없이 바로 사용할 수 있습니다.

## 📁 구조

```
claude-todo-web-pjt/
└── index.html   # 할 일 목록 앱 (HTML+CSS+JS 단일 파일)
```

---
Claude Code로 제작되었습니다.
